<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhzx.server.repository.base.NightStudyDutyGradeDeductionBaseMapper">
    <resultMap id="BaseResultMap" type="com.zhzx.server.domain.NightStudyDutyGradeDeduction">
        <id column="id" jdbcType="BIGINT" property="id" />
        <result column="night_study_duty_id" jdbcType="BIGINT" property="nightStudyDutyId" />
        <result column="grade_id" jdbcType="BIGINT" property="gradeId" />
        <result column="Description" jdbcType="VARCHAR" property="Description" />
    </resultMap>
    <resultMap id="ResultMap" type="com.zhzx.server.domain.NightStudyDutyGradeDeduction" extends="BaseResultMap">
    </resultMap>
    <sql id="Base_Column_List">
        id, night_study_duty_id, grade_id, Description
    </sql>
    <sql id="Base_Column_List_With_Prefix">
        t.id, t.night_study_duty_id, t.grade_id, t.Description
    </sql>
    <insert id="batchInsert">
        insert into day_night_study_duty_grade_deduction (
            night_study_duty_id, grade_id, Description
        )
        values
        <foreach collection="records" item="record" separator=",">
        (
            #{record.nightStudyDutyId, jdbcType=BIGINT}, #{record.gradeId, jdbcType=BIGINT}, #{record.Description, jdbcType=VARCHAR}
        )
        </foreach>
    </insert>
    <update id="updateAllFieldsById" parameterType="com.zhzx.server.domain.NightStudyDutyGradeDeduction">
        update day_night_study_duty_grade_deduction set
        <trim suffixOverrides=",">
            night_study_duty_id = #{et.nightStudyDutyId, jdbcType=BIGINT},
            grade_id = #{et.gradeId, jdbcType=BIGINT},
            Description = #{et.Description, jdbcType=VARCHAR},
        </trim>
        where id = #{et.id}
    </update>
</mapper>
