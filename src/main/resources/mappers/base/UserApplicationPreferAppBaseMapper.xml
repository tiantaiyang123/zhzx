<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhzx.server.repository.base.UserApplicationPreferAppBaseMapper">
    <resultMap id="BaseResultMap" type="com.zhzx.server.domain.UserApplicationPreferApp">
        <id column="id" jdbcType="BIGINT" property="id" />
        <result column="user_id" jdbcType="BIGINT" property="userId" />
        <result column="application_app_id" jdbcType="BIGINT" property="applicationAppId" />
        <result column="sort_order" jdbcType="BIGINT" property="sortOrder" />
    </resultMap>
    <resultMap id="ResultMap" type="com.zhzx.server.domain.UserApplicationPreferApp" extends="BaseResultMap">
        <association property="applicationApp" column="application_app_id" select="queryUserApplicationApp"/>
    </resultMap>
    <select id="queryUserApplicationApp" resultMap="com.zhzx.server.repository.base.ApplicationAppBaseMapper.BaseResultMap">
        select * from sys_application_app where id=#{value}
    </select>
    <sql id="Base_Column_List">
        id, user_id, application_app_id, sort_order
    </sql>
    <sql id="Base_Column_List_With_Prefix">
        t.id, t.user_id, t.application_app_id, t.sort_order
    </sql>
    <insert id="batchInsert">
        insert into sys_user_application_prefer_app (
            user_id, application_app_id, sort_order
        )
        values
        <foreach collection="records" item="record" separator=",">
        (
            #{record.userId, jdbcType=BIGINT}, #{record.applicationAppId, jdbcType=BIGINT}, #{record.sortOrder, jdbcType=BIGINT}
        )
        </foreach>
    </insert>
    <update id="updateAllFieldsById" parameterType="com.zhzx.server.domain.UserApplicationPreferApp">
        update sys_user_application_prefer_app set
        <trim suffixOverrides=",">
            user_id = #{et.userId, jdbcType=BIGINT},
            application_app_id = #{et.applicationAppId, jdbcType=BIGINT},
            sort_order = #{et.sortOrder, jdbcType=BIGINT},
        </trim>
        where id = #{et.id}
    </update>
</mapper>
