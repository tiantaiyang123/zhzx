<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhzx.server.repository.base.NightStudyDutyGradeImagesBaseMapper">
    <resultMap id="BaseResultMap" type="com.zhzx.server.domain.NightStudyDutyGradeImages">
        <id column="id" jdbcType="BIGINT" property="id" />
        <result column="night_study_duty_id" jdbcType="BIGINT" property="nightStudyDutyId" />
        <result column="grade_id" jdbcType="BIGINT" property="gradeId" />
    </resultMap>
    <resultMap id="ResultMap" type="com.zhzx.server.domain.NightStudyDutyGradeImages" extends="BaseResultMap">
        <result column="url" jdbcType="VARCHAR" property="url" />
    </resultMap>
    <sql id="Base_Column_List">
        id, night_study_duty_id, grade_id, url
    </sql>
    <sql id="Base_Column_List_With_Prefix">
        t.id, t.night_study_duty_id, t.grade_id, t.url
    </sql>
    <sql id="Base_Column_List_Full_With_Prefix">
        t.id, t.night_study_duty_id, t.grade_id, t.url
    </sql>
    <insert id="batchInsert">
        insert into day_night_study_duty_grade_images (
            night_study_duty_id, grade_id, url
        )
        values
        <foreach collection="records" item="record" separator=",">
        (
            #{record.nightStudyDutyId, jdbcType=BIGINT}, #{record.gradeId, jdbcType=BIGINT}, #{record.url, jdbcType=VARCHAR}
        )
        </foreach>
    </insert>
    <update id="updateAllFieldsById" parameterType="com.zhzx.server.domain.NightStudyDutyGradeImages">
        update day_night_study_duty_grade_images set
        <trim suffixOverrides=",">
            night_study_duty_id = #{et.nightStudyDutyId, jdbcType=BIGINT},
            grade_id = #{et.gradeId, jdbcType=BIGINT},
            url = #{et.url, jdbcType=VARCHAR},
        </trim>
        where id = #{et.id}
    </update>
</mapper>
