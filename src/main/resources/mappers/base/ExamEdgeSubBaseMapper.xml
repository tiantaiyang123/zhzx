<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhzx.server.repository.base.ExamEdgeSubBaseMapper">
    <resultMap id="BaseResultMap" type="com.zhzx.server.domain.ExamEdgeSub">
        <id column="id" jdbcType="BIGINT" property="id" />
        <result column="exam_id" jdbcType="BIGINT" property="examId" />
        <result column="subject_id" jdbcType="BIGINT" property="subjectId" />
        <result column="base_value" jdbcType="INTEGER" property="baseValue" />
        <result column="float_value" jdbcType="INTEGER" property="floatValue" />
        <result column="upper_value" jdbcType="INTEGER" property="upperValue" />
        <result column="lower_value" jdbcType="INTEGER" property="lowerValue" />
        <result column="create_time" jdbcType="TIMESTAMP" property="createTime" />
        <result column="update_time" jdbcType="TIMESTAMP" property="updateTime" />
    </resultMap>
    <resultMap id="ResultMap" type="com.zhzx.server.domain.ExamEdgeSub" extends="BaseResultMap">
        <association column="exam_id" property="exam" javaType="com.zhzx.server.domain.Exam"
                     select="com.zhzx.server.repository.ExamMapper.selectById" />
    </resultMap>
    <sql id="Base_Column_List">
        id, exam_id, subject_id, base_value, float_value, upper_value, lower_value, create_time, update_time
    </sql>
    <sql id="Base_Column_List_With_Prefix">
        t.id, t.exam_id, t.subject_id, t.base_value, t.float_value, t.upper_value, t.lower_value, t.create_time, t.update_time
    </sql>
    <insert id="batchInsert">
        insert into dat_exam_edge_sub (
            exam_id, subject_id, base_value, float_value, upper_value, lower_value
        )
        values
        <foreach collection="records" item="record" separator=",">
        (
            #{record.examId, jdbcType=BIGINT}, #{record.subjectId, jdbcType=BIGINT}, #{record.baseValue, jdbcType=INTEGER}, #{record.floatValue, jdbcType=INTEGER}, #{record.upperValue, jdbcType=INTEGER}, #{record.lowerValue, jdbcType=INTEGER}
        )
        </foreach>
    </insert>
    <update id="updateAllFieldsById" parameterType="com.zhzx.server.domain.ExamEdgeSub">
        update dat_exam_edge_sub set
        <trim suffixOverrides=",">
            exam_id = #{et.examId, jdbcType=BIGINT},
            subject_id = #{et.subjectId, jdbcType=BIGINT},
            base_value = #{et.baseValue, jdbcType=INTEGER},
            float_value = #{et.floatValue, jdbcType=INTEGER},
            upper_value = #{et.upperValue, jdbcType=INTEGER},
            lower_value = #{et.lowerValue, jdbcType=INTEGER},
        </trim>
        where id = #{et.id}
    </update>
</mapper>
