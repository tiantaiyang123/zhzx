<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhzx.server.repository.StaffMessageReceiverRelationMapper">
    <!-- 根据主键查询 -->
    <select id="selectById" parameterType="java.io.Serializable" resultMap="com.zhzx.server.repository.base.StaffMessageReceiverRelationBaseMapper.ResultMap">
        select * from sys_staff_message_receiver_relation where id = #{id}
    </select>
    <!-- 根据Wrapper查询 -->
    <select id="selectOne" resultMap="com.zhzx.server.repository.base.StaffMessageReceiverRelationBaseMapper.ResultMap">
        select * from sys_staff_message_receiver_relation ${ew.customSqlSegment} LIMIT 1
    </select>
    <insert id="batchInsert">
        insert into sys_staff_message_receiver_relation (
        staff_message_id, receiver_id, receiver_type
        )
        values
        <foreach collection="records" item="record" separator=",">
            (
            #{record.staffMessageId, jdbcType=BIGINT}, #{record.receiverId, jdbcType=BIGINT}, #{record.receiverType, jdbcType=VARCHAR}
            )
        </foreach>
    </insert>
</mapper>
