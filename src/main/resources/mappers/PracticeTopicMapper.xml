<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhzx.server.repository.PracticeTopicMapper">
    <!-- 根据主键查询 -->
    <select id="selectById" parameterType="java.io.Serializable" resultMap="com.zhzx.server.repository.base.PracticeTopicBaseMapper.ResultMap">
        select * from dat_practice_topic where id = #{id}
    </select>
    <!-- 根据Wrapper查询 -->
    <select id="selectOne" resultMap="com.zhzx.server.repository.base.PracticeTopicBaseMapper.ResultMap">
        select * from dat_practice_topic ${ew.customSqlSegment} LIMIT 1
    </select>

    <select id="selectPage" resultMap="com.zhzx.server.repository.base.PracticeTopicBaseMapper.ResultMap">
        select * from dat_practice_topic ${ew.customSqlSegment}
    </select>

    <insert id="batchInsert">
        insert into dat_practice_topic (
        practice_id, clazz_id, topic_name, topic_number, topic_type, score, difficulty, distinguish, grade_average, grade_rate, average, rate, rank
        )
        values
        <foreach collection="records" item="record" separator=",">
            (
            #{record.practiceId, jdbcType=BIGINT},
            #{record.clazzId, jdbcType=BIGINT},
            #{record.topicName, jdbcType=VARCHAR},
            #{record.topicNumber, jdbcType=BIGINT},
            #{record.topicType, jdbcType=VARCHAR},
            #{record.score, jdbcType=DECIMAL},
            #{record.difficulty, jdbcType=DECIMAL},
            #{record.distinguish, jdbcType=DECIMAL},
            #{record.gradeAverage, jdbcType=DECIMAL},
            #{record.gradeRate, jdbcType=DECIMAL},
            #{record.average, jdbcType=DECIMAL},
            #{record.rate, jdbcType=DECIMAL},
            #{record.rank, jdbcType=BIGINT}
            )
        </foreach>
    </insert>
</mapper>
