<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhzx.server.repository.MessageTaskMapper">

    <resultMap id="ResultMap" type="com.zhzx.server.dto.MessageTaskDto" extends="com.zhzx.server.repository.base.MessageTaskBaseMapper.ResultMap">
        <result column="isSend" jdbcType="VARCHAR" property="isSend" />
    </resultMap>
    <!-- 根据主键查询 -->
    <select id="selectById" parameterType="java.io.Serializable" resultMap="com.zhzx.server.repository.base.MessageTaskBaseMapper.ResultMap">
        select * from mes_message_task where id = #{id}
    </select>
    <!-- 根据Wrapper查询 -->
    <select id="selectOne" resultMap="com.zhzx.server.repository.base.MessageTaskBaseMapper.ResultMap">
        select * from mes_message_task ${ew.customSqlSegment} LIMIT 1
    </select>
    <!-- 根据Wrapper查询 -->
    <select id="selectPage" resultMap="com.zhzx.server.repository.base.MessageTaskBaseMapper.ResultMap">
        select
        <include refid="com.zhzx.server.repository.base.MessageTaskReceiverBaseMapper.Base_Column_List" />
        from mes_message_task ${ew.customSqlSegment}
    </select>
    <!-- 根据Wrapper查询 -->
    <select id="selectList" resultMap="com.zhzx.server.repository.base.MessageTaskBaseMapper.ResultMap">
        select * from mes_message_task ${ew.customSqlSegment}
    </select>
    <select id="getPage" resultMap="ResultMap">
        select
        <include refid="com.zhzx.server.repository.base.MessageTaskBaseMapper.Base_Column_List" />,
        (select case when mes_message.id > 0 then 'true' else 'false' END  from mes_message where mes_message.message_task_id = mes_message_task.id and mes_message.is_send = 'YES' LIMIT 1) as isSend
        from mes_message_task ${ew.customSqlSegment}
    </select>
</mapper>
